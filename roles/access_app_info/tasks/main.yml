---
- name: Ensure cloudflare access application information is gathered
  tags:
    - access_app_info
  ansible.builtin.uri:
    headers:
      Authorization: "Bearer {{ access_app_info_api_token }}"
    method: GET
    status_code:
      - 200
    url:
      "{{ 'https://api.cloudflare.com/client/v4/accounts/' ~
          access_app_info_account_id ~
          '/access/apps' }}"
  register: __access_app_info_query
  when:
    - access_app_info_account_id is not none
    - access_app_info_api_token is not none

- name: Ensure list of cloudflare access application information is generated
  tags:
    - access_app_info
  ansible.builtin.set_fact:
    _access_app_info_list:
      "{{ __access_app_info_query.json.result |
          d([]) }}"

- name: Ensure dict of cloudflare access application information is generated
  tags:
    - access_app_info
  ansible.builtin.set_fact:
    _access_app_info_dict:
      "{{ (dict(_access_app_info_list |
               json_query('[].name') |
               zip(_access_app_info_list))) |
          d({}) }}"
...
