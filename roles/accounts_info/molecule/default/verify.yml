---
- name: Verify
  hosts: all
  roles:
    - accounts_info

  post_tasks:
    - name: Ensure info is verified
      ansible.builtin.assert:
        that:
          - _accounts_info_id is defined
          - _accounts_info_name is defined
          - _accounts_info_settings is defined
          - _accounts_info_type is defined
...
