---
- name: Converge
  hosts: all
  roles:
    - tunnel_info

    - role: tunnel_info
      tunnel_info_account_id: "{{ lookup('env', 'CLOUDFLARE_ACCOUNT_ID') }}"
      tunnel_info_api_token: "{{ lookup('env', 'CLOUDFLARE_API_TOKEN') }}"
...
